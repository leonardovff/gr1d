import { FeedbackErrorInputPipe } from './feedback-error-input.pipe';

describe('FeedbackErrorInputPipe', () => {
  it('create an instance', () => {
    const pipe = new FeedbackErrorInputPipe();
    expect(pipe).toBeTruthy();
  });
});
